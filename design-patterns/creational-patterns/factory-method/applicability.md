# When to use

- When you dobn't know the types/dependencies of objects the code should work with

- Want to provide users with a way to extend its internal components

- Save system resources by reusing existing objects instead of rebuilding them each time